### NOW ###
    ...

### SOON ###
    add statement parser

    make bracket parsers and statement parser recursive somehow
        when a group token is encountered
        then parser should run on its tokens
        and replace its tokens

    add entity parsers

    parsing order
        1. curly bracket parsing
        2. statement parsing
        3. round bracket parsing
        4. square bracket parsing
        5. entity parsing

### LATER ###
    ...
